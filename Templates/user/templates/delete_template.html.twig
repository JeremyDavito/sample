{% import "macros/icones.html.twig" as icons %}

<template id="templateUserDelete">
  <!-- Modal content -->
  <div class="pmodal-background" data-controller="userDelete" data-user-id>
    <div class="pmodal">
      <!-- header -->
      <div class="modal_header">
        <span class="title">Supprimer <span class="name">-</span></span>
        <span class="close" data-action="click->userDelete#close">&times;</span>
      </div>
      <!-- header end -->

      <!-- row -->
      <div class="delete-container">
        <div class="delete-row">
          <span>Êtes-vous sûr·e de vouloir supprimer : <span class="name"></span> ?</span>
        </div>
      </div>
      <!-- row end -->

      <!-- footer -->
      <div class="footer-btn">
        <button class="form-btn cancel-btn" data-action="click->userDelete#close">Annuler</button>
        <button class="form-btn delete-btn" data-action="click->userDelete#submit">Supprimer</button>
        <button disabled class="form-btn wait-btn">{{ icons.basic_loading('hw-svg-20') }}&nbsp;Patienter</button>
      </div>
      <!-- footer end -->
    </div>

  </div>
</template>